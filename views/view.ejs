<%- include("partials/header.ejs") %>

<div class="min-vh-100">
  <div class="container my-5">
    <div class="row w-100 flex-row justify-content-center">
        <div class="col-12 p-4 col-lg-8 content ">
            <h1 class="fw-bold mb-3"><%- title %></h1>
            <p class=""><%- content %></p>
        </div>
    </div>
        <div class="content col-lg-2 col-8 my-4 mx-4 p-4 text-center">
            <form action="/delete/<%= postId %>" method="POST" class="mb-3">
                <button class="btn btn-danger btn-lg w-75">Delete</button>
            </form>
            <a href="/edit/<%= postId %>">
                <button class="btn btn-secondary btn-lg w-75">Edit</button>
            </a>
        </div>
  </div>
</div>

<%- include("partials/footer.ejs") %>